http:
 #region routers
  routers:
    pve1:
      entryPoints:
        - "https"
      rule: "Host(`pve1.merox.dev`)"
      middlewares:
        - default-headers
        - https-redirectscheme
      tls: {}
      service: pve1
#    cloud:
#      entryPoints:
#        - "https"
#      rule: "Host(`cloud.merox.dev`)"
#      middlewares:
#        - middlewares-authentik@file
#        - default-headers
#      tls: {}
#      service: cloud
#    proxmox:
#      entryPoints:
#        - "https"
#      rule: "Host(`proxmox.merox.dev`)"
#      middlewares:
#        - default-headers
#        - https-redirectscheme
#      tls: {}
#      service: proxmox
#    dns:
#      entryPoints:
#        - "https"
#      rule: "Host(`dns.merox.dev`)"
#      middlewares:
#        - default-headers
#        - https-redirectscheme
#      tls: {}
#      service: dns
#    pdf:
#      entryPoints:
#        - "https"
#      rule: "Host(`pdf.merox.dev`)"
#      middlewares:
#        - default-headers
#        - https-redirectscheme
#      tls: {}
#      service: pdf



#    testava:
#      entryPoints:
#        - "https"
#      rule: "Host(`testava.merox.dev`)"
#      middlewares:
#        - default-headers
#        - https-redirectscheme
#      tls: {}
#      service: testava



#endregion
#region services
  services:
    pve1:
      loadBalancer:
        servers:
          - url: "https://10.57.57.251:8006"
        passHostHeader: true
#    proxmox:
#      loadBalancer:
#        servers:
#          - url: "https://10.57.57.251:8006"
#       passHostHeader: true
#    dns:
#      loadBalancer:
#        servers:
#          - url: "http://192.168.57.2:3007"
#        passHostHeader: true
#    pdf:
#      loadBalancer:
#        servers:
#          - url: "http://192.168.57.2:8057"
#        passHostHeader: true



#    testava:
#      loadBalancer:
#        servers:
#          - url: "http://192.168.57.2:8080"
#        passHostHeader: true

#endregion
  middlewares:

    https-redirectscheme:
      redirectScheme:
        scheme: https
        permanent: true

    middlewares-authentik:
      forwardAuth:
        address: "http://authentik-server:9000/outpost.goauthentik.io/auth/traefik"
        trustForwardHeader: true
        authResponseHeaders:
          - X-authentik-username
          - X-authentik-groups
          - X-authentik-email
          - X-authentik-name
          - X-authentik-uid
          - X-authentik-jwt
          - X-authentik-meta-jwks
          - X-authentik-meta-outpost
          - X-authentik-meta-provider
          - X-authentik-meta-app
          - X-authentik-meta-version


    default-headers:
      headers:
        frameDeny: true
        browserXssFilter: true
        contentTypeNosniff: true
        forceSTSHeader: true
        stsIncludeSubdomains: true
        stsPreload: true
        stsSeconds: 15552000
        customFrameOptionsValue: SAMEORIGIN
        customRequestHeaders:
          X-Forwarded-Proto: https

    default-whitelist:
      ipAllowList:
        sourceRange:
        - "10.0.0.0/8"
        - "192.168.0.0/16"
        - "172.16.0.0/12"

    secured:
      chain:
        middlewares:
        - default-whitelist
        - default-headers
